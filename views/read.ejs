<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body style="background-color: rgb(23, 22, 22);">
    <div class="w-full h-full p-10 text-white">
        <div class="nav mb-10">
            <a href="/">
                <button class="p-3 text-lg rounded-lg bg-blue-500">Create User</button>
            </a>
        </div>
        <h1 class="text-3xl tracking-tighter mb-5">All Users</h1>
        <div class="users flex flex-wrap gap-3 pb-10">
            <% if (users.length > 0) {%>
                <% users.forEach(function(user) {%>
                    <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
                        <div class="w-full h-72 bg-blue-500 overflow-hidden rounded-lg">
                            <img class="w-full h-full object-cover object-top" src="<%= user.image %>" alt="profile">
                        </div>
                        <h3 class="text-2xl tracking-tighter mt-3"><%= user.name %></h3>
                        <h5 class="text-zinc-500"><%= user.email %></h5>
                        <div class="flex gap-3 mt-10">
                            <a class="text-yellow-300" href="/edit/<%= user._id %>">Edit User</a>
                            <a class="text-red-500" href="/delete/<%= user._id %>">Delete User</a>
                        </div>
                    </div>
                <% }) %>
            <% } else {%>
                <h3 class="text-zinc-600">No users yet</h3>
            <% } %>  
            
        </div>
    </div>
</body>
</html>